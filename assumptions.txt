1) Assuming that the table, which the user sends via payload, already exists
2) Type of the file is a csv file
3) Currently assumed, the database columns are all text (since csv stores everything in text mode)